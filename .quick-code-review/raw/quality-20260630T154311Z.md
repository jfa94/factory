# quality-reviewer — 8f2614a...HEAD

**Status:** DONE
**Verdict:** REQUEST_CHANGES

## Findings

### [important] src/cli/subcommands/scaffold.ts:592 — e2e.blocking deadlocks every task→staging merge

- Quote: `if (blocking) await ensureRequiredCheck(opts, E2E_CHECK_CONTEXT);`
- Why: with `e2e.blocking=true`, scaffold persists the `"E2E"` context into the GLOBAL `git.requiredStatusChecks` overlay (`ensureRequiredCheck` → `saveRawConfig(setAtPath(..., ["git","requiredStatusChecks"], updated))`). Run create reuses that same global list to protect the per-run staging branch (`run.ts:395 requiredChecks: stagingDeps.config.git.requiredStatusChecks`, `strict:true`). The scaffolded `e2e.yml` only runs on develop PRs (`e2e.yml:4 branches: [develop]`), so it NEVER reports an `"E2E"` status on a task PR whose base is `staging-<run-id>`. `shipTask` squash-merges via `gh pr merge <n> --squash` with NO `--admin`; GitHub blocks the merge because the required `"E2E"` check is permanently "Expected — waiting for status". Every task ship in an `e2e.blocking` repo wedges; no run can reach `done`/finalize. Default config (`requiredStatusChecks=[]`) is unaffected — the regression is specific to opting into `e2e.blocking`.
- Scenario: scaffold with `e2e.blocking=true` → `"E2E"` written to `git.requiredStatusChecks`; `factory run` cuts `staging-<id>` requiring `"E2E"` (strict); first task ships → `gh pr merge --squash` rejected because the develop-only `e2e.yml` never reported `"E2E"` on the staging-base PR → run stuck.
- Fix sketch: keep `"E2E"` out of the per-run staging `requiredChecks` — thread a develop-only check list to develop protection, or subtract `"E2E"` from staging checks at run-create, or scaffold `e2e.yml` to also trigger on `staging-*` bases.

Chain anchors:

- `src/cli/subcommands/scaffold.ts:592` — `if (blocking) await ensureRequiredCheck(opts, E2E_CHECK_CONTEXT);` (writes E2E into global overlay)
- `src/cli/subcommands/run.ts:395` — `requiredChecks: stagingDeps.config.git.requiredStatusChecks,` (staging inherits the required check)
- `templates/ci/e2e.yml:4` — `branches: [develop]` (never runs on staging-base PRs)
