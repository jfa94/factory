# architecture-reviewer — 8f2614a...HEAD

**Status:** DONE
**Verdict:** WARNING

Layering sound: no new circular deps (madge clean, 308 files); `shared/e2e-recommendation.ts` placed downward so both orchestrator (writer) and scoring (reporter) depend on it, avoiding a latent `scoring → orchestrator → scoring` cycle; the run-level E2E phase mirrors the existing `docs` phase; both new deps (`@playwright/mcp@0.0.77`, `@playwright/test@1.61.1`) are real and pinned. Dropped as defensible-by-design: the finalize-only terminal-status override and the narrowed `E2eFindingLine` view-model in scoring.

## Findings

### [important] scripts/factory-run-runner.js:524 — Workflow runner re-implements the engine's E2E verdict↔findings invariant (normalizeSweep), diverging from the session path

- Quote: `function normalizeSweep(sweep) {`
- Why: Model-A contract (CLAUDE.md) says a runner "carries no pipeline logic of its own." `normalizeSweep` (524-529) derives the sweep verdict from findings — an inverted re-implementation of the engine's `E2eSweepResultSchema.superRefine` (`src/orchestrator/e2e-sweep.ts:102-132`). The two runners therefore handle an identical self-contradictory agent result differently: the session `--results` boundary (`run.ts:988 E2eSweepResultSchema.parse`) THROWS and halts loud; the workflow path silently REPAIRS, and for a `blocked` verdict carrying no blocking finding relaxes to `approve` (line 528). No lockstep guard pins the duplicated invariant, so superRefine changes will drift `normalizeSweep`. The nearby `parseEnvelope` mirror has a tested source-of-truth lockstep — this repair has none.
- Fix sketch: Route the workflow result through the same engine validation, or extract the verdict↔findings derivation into one shared, tested helper both `normalizeSweep` and `superRefine` consume.
