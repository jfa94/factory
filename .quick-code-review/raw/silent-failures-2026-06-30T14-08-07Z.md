# silent-failure-hunter — raw output (2026-06-30T14-08-07Z)

Status: DONE

## Findings (1)

### [important] src/orchestrator/finalize.ts:184 — Session-mode finalize merges a run with recorded blocking money-path findings — no engine re-verification, only the LLM runner's discretion

> **REFUTED (dropped):** The finding describes the documented, intentional design, not a silent failure. The semantic sweep is defined as "a run-level, NON-BLOCKING, runner-adjudicated step... The engine never refuses the rollup — the runner adjudicates the recommendation" (src/orchestrator/e2e-sweep.ts:14-16). The session-vs-workflow asymmetry the reviewer flags is deliberate and explained in four places: (a) src/orchestrator/next.ts:124-125 — "Session `done` (even blocking) still returns false: it adjudicates fix-forward in-loop, and re-sweeping it would be an unbounded autonomous re-sweep loop"; (b) src/orchestrator/e2e-sweep.ts:258-263 — "Workflow mode has no in-loop adjudicator: a blocking money-path must HALT durably," with `suspendOnBlocking = run.mode === "workflow" && blockingCount > 0`; (c) the finalize backstop at finalize.ts:176-181 is explicitly scoped "workflow only" as "the sole catch when the sweep gate is disabled... between halt and resume"; (d) skills/pipeline-runner/SKILL.md:190-199 documents the session runner's `fix-forward` adjudication loop ("/factory:debug fix-forward loop... The engine does NOT auto-re-run the sweep (v1)... NEVER auto-refuse the rollup"). Session mode is the interactive, in-session LLM-runner mode (per CLAUDE.md), so the in-loop adjudicator IS the engine's chosen verification surface; workflow mode gets the engine backstop precisely because it has no such adjudicator. Moreover it is not "silent": blocking findings are surfaced to the runner in the done envelope (e2e-sweep.ts:48-49,278-281), persisted durably to runs/<run-id>/e2e-findings.json (e2e-sweep.ts:254-257), and surfaced in the merged rollup PR body — finalize.ts:197-211 reads them on status==="done" for BOTH modes and feeds buildPartialReport whose markdown is the rollup body (finalize.ts:208-211,240; SKILL.md:197-199). The reviewer's premise that the engine should re-verify/hard-block in session mode directly contradicts the stated contract that the engine never refuses the rollup and the runner adjudicates in-loop.

- Quote: `run.mode === "workflow" &&`
- Why: The fail-closed money-path backstop is gated on run.mode === "workflow" (finalize.ts:182-192), so a SESSION-mode run is exempt. Session mode is the documented default (CLAUDE.md: "default session+live"). Trace: (1) the sweep records e2e_sweep.status="done" with blocking_count=N>0 (e2e-sweep.ts:264-274). (2) next.ts wantsSemanticSweep returns false for a session `done` sweep even when blocking (next.ts:124-126 — "Session `done` (even blocking) still returns false"), so the run advances straight to docs→finalize. (3) The engine NEVER re-runs the sweep in session mode (the only e2e_sweep writers are in e2e-sweep.ts; SKILL.md states "The engine does NOT auto-re-run the sweep"), so blocking_count remains its STALE original value whether or not the runner actually fixed the money-path. (4) finalize.ts:226-269 then opens the rollup PR, merges to develop (merge: shipMode==="live"), posts the prdDoneComment "PRD delivered — all N task(s) shipped" (finalize.ts:65-68, which never mentions money-path findings), and closes the PRD. The whole feature exists to fix "a blocking money-path ships invisibly under a clean report" (partial-report.ts:71-74), yet in the default session mode a genuinely-broken blocking money-path ships to the integration branch and the PRD closes as delivered, with the only barrier being an LLM runner's non-enforceable adjudication. Failure scenario: an autonomous session run finds 2 blocking checkout failures, the session runner mis-judges them as false positives (or its bounded fix-forward fails) and advances; finalize merges the broken checkout to develop and closes the PRD as delivered. Partial mitigation (why this is important, not critical): the findings ARE surfaced in the report.e2eSweep section / rollup PR body — but because blocking_count is never re-verified, that section shows the same stale blocking findings for a fixed money-path as for a shipped-broken one, so a reviewer can't distinguish the two and is trained to ignore them.
- Fix sketch: Either (a) apply the same fail-closed backstop to session mode (re-sweep or refuse to auto-merge while blocking_count>0), or (b) have the session runner re-record the sweep after a fix-forward so blocking_count reflects the post-fix state, and/or (c) include the unresolved blocking-money-path count in prdDoneComment so the PRD-delivered message is not a clean-success signal over a recorded blocking failure.

