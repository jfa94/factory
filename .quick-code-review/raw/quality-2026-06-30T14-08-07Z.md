# quality-reviewer — raw output (2026-06-30T14-08-07Z)

Status: DONE

## Verdict

The semantic money-path sweep is implemented to a high standard — crash-resume idempotency, derive-don't-store, durable workflow halt + finalize backstop, and surfaced best-effort caps all check out. One genuine minor robustness gap survived verification: the runner's retryable SWEEP_OUT author gate is more permissive than the engine's no-retry `--results` boundary, which the gate's own comment forbids.

## Findings (1)

### [minor] scripts/factory-run-runner.js:176 — SWEEP_OUT author gate omits the verdict↔blocking cross-field check the engine enforces, so a self-contradictory sweep passes the retryable gate then dies at the no-retry --results boundary

- Quote: `verdict: { type: "string", enum: ["approve", "blocked", "error"] },`
- Failure mode: invariant-without-repair
- Scenario: The opus e2e-author emits {verdict:"approve", journeys_total:3, findings:[{...,blocking:true}]} (an LLM self-contradiction despite the prompt's 'blocked iff any blocking:true' instruction). SWEEP_OUT accepts it (no cross-field rule) → recordEnvelope writes it and runs `factory run e2e --results <path>` → the engine superRefine throws 'verdict \'approve\' is illegal with a blocking finding' → parseEnvelope(out.raw, E2E_KINDS) finds no valid envelope and throws → the workflow run halts with an unhandled error rather than retrying the author.
- Why: SWEEP_OUT validates the sweep result's STRUCTURE only — it has no cross-field rule tying `verdict` to the findings' `blocking` flags. The engine's E2eSweepResultSchema.superRefine (src/orchestrator/e2e-sweep.ts:105-121) DOES, rejecting `approve` with a blocking finding and `blocked` with none. recordEnvelope's `--results` call is explicitly NO-RETRY (a re-spawn could double-record), so a contradiction that slips past the gate throws inside `factory run e2e --results`, parseEnvelope cannot read the error as an envelope, and the whole workflow runner halts ungracefully — wasting a full opus+app-boot sweep and requiring a re-trigger — instead of re-prompting the author the way the gate is meant to. The SWEEP_OUT comment itself states the gate 'must never be more permissive than the no-retry `--results` boundary', but only the `status` enum was mirrored, not the verdict↔blocking invariant.
- Fix sketch: After the author returns in runE2e(), add a small JS cross-field guard mirroring the superRefine (reject approve+blocking and blocked+no-blocking, treat like a schema miss so `agent` re-prompts) BEFORE recordEnvelope writes the file — keeping the retryable gate at least as strict as the no-retry boundary, as its own comment requires.
- Anchors:
  - scripts/factory-run-runner.js:176 — `verdict: { type: "string", enum: ["approve", "blocked", "error"] },` (permissive retryable gate (no cross-field check))
  - src/orchestrator/e2e-sweep.ts:109 — `message: "verdict 'approve' is illegal with a blocking finding — use 'blocked'",` (strict no-retry boundary that throws on the contradiction)
  - scripts/factory-run-runner.js:197 — `This retryable author gate must never` (the stated invariant this gap violates)

