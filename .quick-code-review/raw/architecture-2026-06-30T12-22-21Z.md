# architecture-reviewer — raw output (2026-06-30T12-22-21Z)

**Status:** DONE

**Verdict:** WARNING — the intentional cycle-break is verified working (madge: "No circular dependency found!"; scoring/partial-report.ts now imports the zero-import leaf orchestrator/e2e-recommendation.ts instead of e2e-sweep.ts, severing deps.ts→scoring→partial-report→e2e-sweep). All other new import edges mirror the established docs-phase pattern (next.ts MAX_E2E_SWEEP_ATTEMPTS ↔ MAX_DOCS_ATTEMPTS, index.ts barrel re-export, workflow-envelope E2eSweepAction ↔ DocsAction). One minor structural smell: the leaf helper lives in orchestrator/ but is consumed by scoring/, giving the two packages a directory-level bidirectional edge that file-level madge cannot catch.

## [minor] src/scoring/partial-report.ts:25 — Cycle-break leaf lives in orchestrator/ but is consumed by scoring/, creating a directory-level bidirectional dependency

- kind: local
- scenario: A future edit adds any import to src/orchestrator/e2e-recommendation.ts (e.g. pulling a shared type from a sibling orchestrator module) — the file is no longer a leaf, the scoring→orchestrator→scoring file cycle reappears, and check:circular fails for a change that looks unrelated to scoring.
- verbatim: `import { e2eRecommendation } from "../orchestrator/e2e-recommendation.js";`
- why: The cycle was severed at the file level only. orchestrator/deps.ts:103 imports `} from "../scoring/index.js";` (orchestrator → scoring), while scoring/partial-report.ts:25 imports `e2eRecommendation` from `../orchestrator/e2e-recommendation.js` (scoring → orchestrator). madge sees no cycle solely because e2e-recommendation.ts has zero imports, but the two packages now point at each other. A pure helper consumed by both layers belongs in the clean lower layer src/shared/ (verified import-free of orchestrator/scoring), which partial-report.ts already depends on (line 24, `from "../shared/index.js"`). Placing it there removes the back-edge entirely instead of relying on a leaf to dodge the detector.
- fix_sketch: Move e2eRecommendation into src/shared/ (or src/scoring/); update the two importers (e2e-sweep.ts:24 and partial-report.ts:25). Both orchestrator and scoring then depend downward on shared, eliminating the back-edge structurally rather than by leaf-discipline.

