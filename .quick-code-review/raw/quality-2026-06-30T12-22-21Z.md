# quality-reviewer — raw output (2026-06-30T12-22-21Z)

**Status:** DONE

**Verdict:** REQUEST_CHANGES

## [important] scripts/factory-run-runner.js:196 — SWEEP_OUT author-boundary schema accepts finding status "pass" that the engine's no-retry record rejects, defeating the retry safety net

- kind: local
- scenario: e2e-author emits findings:[{...,"status":"pass","blocking":false}] for a clean journey; SWEEP_OUT accepts it, record-time E2eSweepResultSchema.parse rejects "pass", the no-retry record throws and the whole run ends loud instead of re-prompting the author.
- verbatim: `status: { type: "string", enum: ["pass", "fail", "uncertain"] },`
- why: PREMISE: The SWEEP_OUT JSON schema is declared to "Mirror the engine's E2eSweepResultSchema ... so a malformed sweep result is rejected — and retried — at the author boundary" (scripts/factory-run-runner.js:1006-1007). The author boundary is the ONLY retryable validation; the subsequent record (`factory run e2e --results`) is explicitly NO-RETRY. EVIDENCE: SWEEP_OUT allows finding `status` ∈ {"pass","fail","uncertain"} (line 196), but the engine's E2eJourneyFindingSchema is `status: z.enum(["fail", "uncertain"])` (src/orchestrator/e2e-sweep.ts:87) — "pass" is forbidden because `findings` lists problems only. TRACE: an e2e-author result that includes a passing journey as a finding with status:"pass" (the agent is even told at agents/e2e-author.md:122 to "Record the journey result: pass/fail/uncertain") passes the retryable SWEEP_OUT validation in runE2e(); recordEnvelope() then runs `factory run e2e --results <file>` which calls E2eSweepResultSchema.parse() (src/cli/subcommands/run.ts:1775) → throws → CLI exits non-zero → recordEnvelope is single-attempt (no retry) → run dies loud. CONCLUSION: the mirror is more permissive than the engine in exactly the direction that converts a recoverable, author-boundary-retryable mistake into an unrecoverable run death. The same gap exists for the verdict↔blocking cross-field invariants (E2eSweepResultSchema.superRefine) which SWEEP_OUT does not enforce.
- fix_sketch: Tighten SWEEP_OUT findings status enum to ["fail","uncertain"] to match the engine, or strip status:"pass" findings before record; ideally derive the JS mirror from the same source the engine uses so it cannot drift more-permissive than the no-retry boundary.

## [important] scripts/factory-run-runner.js:618 — Workflow-mode fix-forward halt is non-durable: the sweep is already recorded `done` and the run is left `running`, so `resume` skips the gate and ships the blocking money-path

- kind: local
- scenario: Workflow run, semantic sweep finds 1 blocking checkout failure → runner returns suspended. User reads the rollup and runs `factory resume` (per the printed instruction) → engine sees e2e_sweep done, skips the sweep, finalize merges the rollup to base with the broken checkout shipped.
- verbatim: `if (e.recommendation === "fix-forward") {`
- why: PREMISE: The halt exists "so a blocking money-path never ships invisibly" and instructs the human to "review the rollup + re-run before shipping" (lines 619-627). EVIDENCE: by the time this branch runs, runE2eRecord has ALREADY persisted `e2e_sweep:{status:"done",...}` and did NOT set run.status (src/orchestrator/e2e-sweep.ts:3515-3524 — the done path only updates e2e_sweep, never status). The workflow merely returns a `{suspended:true}` document; no CLI writes run.status="suspended". TRACE: on the promised re-run, `factory run resume` reads state.json with status still "running" and e2e_sweep.status==="done"; wantsSemanticSweep() returns false when `run.e2e_sweep?.status === "done"` (src/orchestrator/next.ts:4017) → nextTask falls through to docs/finalize → finalizeRun ships the rollup (the semantic sweep is non-blocking and never gates the merge). CONCLUSION: re-running after the halt does NOT re-sweep or re-gate — it ships the exact blocking money-path the halt claimed to protect against, and the run is meanwhile left in a non-terminal `running` state that was never persisted as suspended.
- fix_sketch: Before halting in workflow mode on fix-forward, either do NOT record the sweep as `done` (leave it resumable so re-run re-sweeps) or have the engine persist run.status="suspended" with a sticky blocking-sweep flag that finalize/next refuses to advance past until cleared.

