# security-reviewer — raw output (2026-06-30T12-22-21Z)

**Status:** DONE

**Verdict:** CONDITIONAL — one MEDIUM credential-leak path (auth setup project inherits trace=retain-on-failure and is uploaded by the nightly artifact); everything else (SHA-pinned actions, contents:read scoping, pull_request vs pull_request_target, JSON.stringify escaping of scaffold substitutions, payload-as-inert-data handoff) checks out.

## [important] src/cli/subcommands/scaffold.ts:490 — Auth setup project leaks the test password into the nightly playwright-report artifact

- kind: local
- scenario: Repo enables e2e.semantic, uses auth.setup.ts Option B (E2E_USER/E2E_PASS UI login) rather than authSetupCommand. A login regression makes auth.setup.ts fail in the nightly run → retain-on-failure writes a trace capturing fill(pass) → trace is bundled into playwright-report/ → uploaded as `playwright-report-nightly` for 7 days, exposing the test account password to anyone with repo read access.
- verbatim: `{ name: "setup", testMatch: /auth\\.setup\\.ts/ },`
- why: The scaffolded `setup` project (which runs auth.setup.ts) is emitted with NO `use`/`trace` override, so it inherits the base `trace: "retain-on-failure"` from templates/playwright.config.ts:21. The sibling `semantic-*` projects were deliberately given `trace: "off"` at scaffold.ts:503 precisely because a retained trace is an uploadable artifact that leaks session secrets — but the very project that handles raw credentials was overlooked. auth.setup.ts Option B drives the UI login and types the password at templates/tests/e2e/auth.setup.ts:60 (`await page.getByLabel("Password").fill(pass);`), whose value is recorded in the Playwright trace action log. templates/ci/e2e-nightly.yml runs `npx playwright test` (all projects, incl. setup) at line 29 and unconditionally (`if: ${{ !cancelled() }}`) uploads `playwright-report/` (lines 30-34) with 7-day retention; the HTML reporter copies failed-test trace attachments into playwright-report/data/, so a failed login persists the typed E2E_PASS to a downloadable artifact (publicly downloadable on a public repo).
- fix_sketch: Emit the `setup` project with `use: { trace: "off" }` (mirror the semantic-* override at scaffold.ts:503), and/or scope the nightly upload-artifact path to exclude trace attachments. Document that authSetupCommand (Option A, no in-browser credentials) is the safer default.

