# architecture-reviewer — raw output

**Status:** DONE
**Verdict:** APPROVE — Architecture is sound. madge confirms zero circular dependencies across the tree; the new semantic-sweep feature mirrors the existing docs.ts run-level-phase pattern, and the deliberate extraction of e2eRecommendation into the shared/ leaf provably avoids a scoring↔orchestrator cycle (scoring/partial-report.ts imports nothing from orchestrator). One minor, non-blocking peer-coupling note below.

## Findings (1)

### [minor] src/cli/subcommands/run.ts:61 — run subcommand imports a constant from its peer scaffold subcommand
- kind: local
- quote: `import { E2E_CHECK_CONTEXT } from "./scaffold.js";`
- why: This is a sibling-to-sibling edge between two CLI subcommands (src/cli/subcommands/run.ts -> src/cli/subcommands/scaffold.ts) created solely to share the one-line string constant E2E_CHECK_CONTEXT ("E2E"). It pulls the entire scaffold module (and its git/config/gate-env transitive deps) into run.ts's import graph for a value the run path only uses to filter staging required checks. madge confirms this forms NO cycle and the value's authority is arguably the scaffold (it must match e2e.yml's `name: E2E`), so the coupling is defensible and harmless today — but a shared canonical-names leaf already exists for exactly this class of cross-consumer constant (src/shared/gate-config-names.ts), and the next time run-create needs another scaffold-owned token this peer edge will keep widening. Low severity, no runtime or correctness impact.
- fix: Optional: relocate E2E_CHECK_CONTEXT to a zero-dependency shared leaf (e.g. alongside src/shared/gate-config-names.ts) and have both scaffold.ts and run.ts import it downward, eliminating the subcommand→subcommand edge. Keep a comment tying the constant to e2e.yml's job name.
