{
  "name": "dark-factory",
  "version": "0.2.0",
  "description": "Autonomous coding pipeline: converts GitHub PRD issues into merged pull requests with quality-first review gates",
  "userConfig": {
    "maxRuntimeMinutes": {
      "type": "number",
      "default": 0,
      "min": 0,
      "max": 1440,
      "description": "Maximum pipeline runtime in minutes before circuit breaker trips. 0 = unlimited (default). Set to a positive value to enable a wall-clock emergency brake."
    },
    "maxConsecutiveFailures": {
      "type": "number",
      "default": 5,
      "min": 1,
      "max": 10,
      "description": "Consecutive task failures before pipeline aborts"
    },
    "humanReviewLevel": {
      "type": "number",
      "default": 1,
      "min": 0,
      "max": 4,
      "description": "Autonomy level: 0=full auto, 1=PR approval (default), 2=review checkpoint, 3=spec approval, 4=full supervision"
    },
    "maxParallelTasks": {
      "type": "number",
      "default": 3,
      "min": 1,
      "max": 10,
      "description": "Maximum concurrent task-executor agents"
    },
    "review.routineRounds": {
      "type": "number",
      "default": 2,
      "min": 1,
      "max": 5,
      "description": "Review rounds for routine-tier tasks (cloud)"
    },
    "review.featureRounds": {
      "type": "number",
      "default": 4,
      "min": 1,
      "max": 10,
      "description": "Maximum adversarial review rounds for feature-tier tasks (cloud)"
    },
    "review.securityRounds": {
      "type": "number",
      "default": 6,
      "min": 1,
      "max": 10,
      "description": "Maximum adversarial review rounds for security-tier tasks (cloud)"
    },
    "review.preferCodex": {
      "type": "boolean",
      "default": true,
      "description": "Use Codex adversarial review when available, fall back to Claude Code"
    },
    "quality.holdoutPercent": {
      "type": "number",
      "default": 20,
      "min": 0,
      "max": 50,
      "description": "Percentage of acceptance criteria to withhold for holdout validation"
    },
    "quality.holdoutPassRate": {
      "type": "number",
      "default": 80,
      "min": 50,
      "max": 100,
      "description": "Minimum percent of withheld criteria that must be satisfied"
    },
    "quality.mutationScoreTarget": {
      "type": "number",
      "default": 80,
      "min": 50,
      "max": 100,
      "description": "Minimum mutation score percentage"
    },
    "quality.mutationTestingTiers": {
      "type": "array",
      "default": ["feature", "security"],
      "description": "Risk tiers that require mutation testing (skip for routine)"
    },
    "quality.coverageMustNotDecrease": {
      "type": "boolean",
      "default": true,
      "description": "Block tasks that decrease test coverage"
    },
    "quality.coverageRegressionTolerancePct": {
      "type": "number",
      "default": 0.5,
      "min": 0,
      "max": 10,
      "description": "Maximum allowed drop in coverage (percentage points) before the regression gate fails. Default 0.5 absorbs measurement noise from branch/line count shifts. This is a regression tolerance, NOT a minimum-coverage floor — projects that want to enforce a floor should add a dedicated CI step."
    },
    "execution.defaultModel": {
      "type": "string",
      "default": "sonnet",
      "enum": ["haiku", "sonnet", "opus"],
      "description": "Default model for task execution (overridden by complexity classification)"
    },
    "execution.modelByTier.simple": {
      "type": "string",
      "default": "haiku",
      "enum": ["haiku", "sonnet", "opus"],
      "description": "Model used for tasks classified as simple tier (low file count, no dependencies)"
    },
    "execution.modelByTier.medium": {
      "type": "string",
      "default": "sonnet",
      "enum": ["haiku", "sonnet", "opus"],
      "description": "Model used for tasks classified as medium tier"
    },
    "execution.modelByTier.complex": {
      "type": "string",
      "default": "opus",
      "enum": ["haiku", "sonnet", "opus"],
      "description": "Model used for tasks classified as complex tier (many files or deep dependency chains)"
    },
    "execution.maxTurnsSimple": {
      "type": "number",
      "default": 40,
      "min": 10,
      "max": 200,
      "description": "Max turns for simple/haiku-tier tasks"
    },
    "execution.maxTurnsMedium": {
      "type": "number",
      "default": 60,
      "min": 20,
      "max": 200,
      "description": "Max turns for medium/sonnet-tier tasks"
    },
    "execution.maxTurnsComplex": {
      "type": "number",
      "default": 80,
      "min": 20,
      "max": 200,
      "description": "Max turns for complex/opus-tier tasks"
    },
    "dependencies.prMergeTimeout": {
      "type": "number",
      "default": 45,
      "min": 5,
      "max": 180,
      "description": "Minutes to wait for dependency PR to merge"
    },
    "dependencies.pollInterval": {
      "type": "number",
      "default": 60,
      "min": 10,
      "max": 300,
      "description": "Seconds between merge status polls"
    },
    "observability.auditLog": {
      "type": "boolean",
      "default": true,
      "description": "Enable tamper-evident audit logging of all tool uses"
    },
    "observability.metricsExport": {
      "type": "string",
      "default": "json",
      "enum": ["json", "sqlite"],
      "description": "Metrics storage format"
    },
    "observability.metricsRetentionDays": {
      "type": "number",
      "default": 90,
      "min": 7,
      "max": 365,
      "description": "Days to retain metrics data"
    },
    "safety.writeBlockedPaths": {
      "type": "array",
      "default": [],
      "description": "Glob patterns (bash globstar + extglob) of file paths that the write-protection hook must block. Evaluated on PreToolUse Edit/Write/MultiEdit. Empty by default; add entries like \"**/migrations/**\" or \".env*\" to opt into blocking."
    },
    "safety.useTruffleHog": {
      "type": "boolean",
      "default": false,
      "description": "When true, the secret-commit-guard hook runs `trufflehog filesystem --directory <cwd> --only-verified` before every git commit in addition to the built-in path and regex scans. Findings are filtered against safety.allowedSecretPatterns. If trufflehog is not installed the hook logs a warning and continues with regex-only scanning."
    },
    "safety.allowedSecretPatterns": {
      "type": "array",
      "default": [],
      "description": "Regex patterns for known-safe secret-like strings (e.g. Supabase anon keys, Stripe publishable keys). Any path-scan hit or TruffleHog finding whose raw value matches one of these patterns is filtered out before deciding whether to block a commit."
    }
  }
}
